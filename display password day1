#include <stdio.h>
#include <string.h>

int main() {
    char str[100];

    printf("Enter a string: ");
    scanf("%s", str);

    printf("Password format: ");
    for (int i = 0; str[i] != '\0'; i++) {
        printf("*");
    }

    printf("\nOriginal string: %s\n", str);

    return 0;
}
